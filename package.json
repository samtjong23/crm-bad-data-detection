{
  "name": "crm-bad-data-detection",
  "version": "1.0.0",
  "description": "Data quality tool designed to detect and report on bad data in the contact data of a HubSpot CRM system",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Samuel Tjong",
  "license": "ISC",
  "dependencies": {
    "@hubspot/api-client": "^9.0.0",
    "@slack/web-api": "^6.8.1",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  }
}
